<h1 class="p-3"><%= @list.name %></h2>


<div class="row row-cols-1 row-cols-md-2 g-4">
 <% @list.bookmarks.each do |bookmark| %>
  <div class="col">
    <div class="card">
      <img src="<%= bookmark.movie.poster_url%>" class="card-img-top" alt="...">
      <div class="card-body">
        <h5 class="card-title"><%= bookmark.movie.title%></h5>
        <p class="card-text"><%= bookmark.comment%></p>
          </div>
            <div class="card-footer">
              <small class="text-muted"><%= bookmark.movie.rating%>/10</small>
            </div>
          </div>
        </div>
      <% end %>
    </div>
      <div class="p-3">
        <%= link_to "Ajoute un film chef !", new_list_bookmark_path(@list) %>
      </div>
      <div class="p-3">
        <%= link_to "Retour à la homepage", lists_path %>
      </div>
